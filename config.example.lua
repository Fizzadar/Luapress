local config = {
    --website url
    url = 'http://localhost/Luapress/src/build',
    --build directory
    dir = 'build',
    --website title
    title = 'A Lone Ship',
    --template
    template = 'default',
    --posts per page
    posts_per_page = 2
}

return config
